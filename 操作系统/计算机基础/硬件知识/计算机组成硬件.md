## 计算机组成硬件

#### 计算机的组成

计算机五大组成：运算器、控制器、存储器、输入设备和输出设备。



先了解内存

```
内存（英语：Random Access Memory，缩写：RAM；也叫主存）是与CPU直接交换数据的内部存储器。

内存用来加载各式各样的程序与数据以供CPU直接运行与运用。由于DRAM的性价比很高，且扩展性也不错，是现今一般计算机主存的最主要部分。

2011年生产计算机所用的主存主要是DDR3 SDRAM，而2016年开始DDR4 SDRAM逐渐普及化，笔记本电脑厂商开始在笔记本电脑以DDR4存储器取代DDR3L。
```

内存特点

```
内存特点：

1、当电源关闭时RAM不能保留数据。如果需要保存数据，就必须把它们写入一个长期的存储设备中。RAM和ROM相比，两者的最大区别是RAM在断电以后保存在上面的数据会自动消失，而ROM则不会。
```

发展

```
 发展
 DDRSDRAM(Double Data Rate SDRAM）简称DDR，也是“双倍速率SDRAM”的意思。
▪ 内存条
▪ SDRAM
▪ DDR时代
▪ DDR2时代
▪ RDRAM时代
▪ DDR3时代
▪ DDR4时代
```

分类

```
▪ 按工作原理分类
▪ 按内存技术标准分类
▪ 按系统逻辑分类
▪ 其他类型
```

接口类型

```
▪ DIP
▪ SIMM
▪ DIMM
```

技术指标

```
▪ 奇偶校验
▪ 内存容量
▪ 存取时间
▪ CL延迟
▪ 频率
▪ 带宽
```

RAM分为两种：DRAM和SRAM。

```
1.DRAM( Dynamic RAM，动态随机存储器)的存储单元是由电容和相关元件做成的，电容内存储电荷的多寡代表信号0和1。电容存在漏电现象，电荷不足会导致存储单元数据出错，所以DRAM需要周期性刷新，以保持电荷状态。DRAM结构较简单且集成度高，通常用于制造内存条中的存储芯片。 [4] 

2.SRAM( Static RAM，静态随机存储器)的存储单元是由晶体管和相关元件做成的锁存器，每个存储单元具有锁存“0”和“1”信号的功能。它速度快且不需要刷新操作，但集成度差和功耗较大，通常用于制造容量小但效率高的CPU缓存
```

内存频率

```
DDR内存和DDR2内存的频率可以用工作频率和等效频率两种方式表示，工作频率是内存颗粒实际的工作频率，但是由于DDR内存可以在脉冲的上升和下降沿都传输数据，因此传输数据的等效频率是工作频率的两倍；而DDR2内存每个时钟能够以四倍于工作频率的速度读/写数据，因此传输数据的等效频率是工作频率的四倍。
```

带宽

```
从功能上理解，我们可以将内存看作是内存控制器（一般位于北桥芯片中）与CPU之间的桥梁或与仓库。显然，内存的容量决定“仓库”的大小，而内存的带宽决定“桥梁”的宽窄，两者缺一不可，这也就是我们常常说道的“内存容量”与“内存速度”。


带宽=总线宽度×总线频率×一个时钟周期内交换的数据包个数。
```

时序

```
https://blog.csdn.net/wangpengqi/article/details/8351816

内存时序越低  延迟 也就越低。
CL 14

DDR4   3600      CL   14    64G
       内存频率   内存时序    内存大小
```



速度

```
固态硬盘的速度 大约在 7G/s

内存可以跑出 30G/s  read     34G/s    write      30G/s  copy            latency   96.6ns

一级缓存 大约在 950G/s read      485G/s    write      967G/s  copy            latency   1.0 ns
二级缓存 大约在 365G/s read     217G/s    write      346G/s  copy            latency   3.0ns
三级缓存 大约在 230G/s read     160G/s    write      200G/s  copy            latency   11.9ns

根据不同的内存 可能有所差别。

```

 

内存容量

```
内存的容量不存与  cpu 的交互 存在一定的限制
那到底 需要 多大的 内存呢？


听歌 看电影的话 简单办公  只需要  4G 足够了

如果 打游戏 日常办公  大概  8G 16G  就可以了 因为还需要有 显卡 来支撑。

3Dmax  或者 大需要生产力  32G



```



内存频率

```
内存频率是衡量内存速度的重要指标。 以Mhz为单位。
内存频率越大 性能也越好 价格也越贵。

那到底 需要买 多大的 内存条呢？

还需要根据主板的参数信息 来购买  不然 带不动。。。
有的主板支持超频  有的不支持  有的最大支持  内存的 频率限制。（超过 就向下 兼容 ）

AMD 全系 支持超频

其他 要根据主板型号


```

赫兹

```
赫兹：
每秒震动的次数   一秒震动一次  就是 1赫兹   指每秒发生周期波动的次数1千赫 （kHz 103 Hz） =1 000 Hz [9] 

1兆赫 （MHz 106 Hz） =1 000 000 Hz [9] 
1吉赫 （GHz 109 Hz） =1 000 000 000 Hz [8] 
1千赫 （kHz 103 Hz） =1 000 Hz [9] 
1兆赫 （MHz 106 Hz） =1 000 000 Hz [9] 
1吉赫 （GHz 109 Hz） =1 000 000 000 Hz [8] 
1太赫 （THz 1012 Hz） =1 000 000 000 000 Hz [8] 
1拍赫 （PHz 1015 Hz） =1 000 000 000 000 000 Hz [8] 
1艾赫 （EHz 1018 Hz） =1 000 000 000 000 000 000 Hz [8] 

赫兹（hertz）简写为Hz。物理上频率的测量单位，指每秒发生周期波动的次数。高频率的计量单位主要有kHz（即1000Hz）、MHz（即1000kHz）、GHz（即1000MHz）、THz（即1000GHz）。 [3] 
频率是单位时间内周期性过程重复、循环或振动的次数，通常用f来表示。 [6] 
无线电频率通常以千赫（kHz）或兆赫（MHz）来计量。 [7] 
单模光纤的通带是100GHz/km，多模光纤的通带可以超过10GHz/km。 [8] 
```



内存颗粒

```

```





内存超频

```
在主板打开 bios 设置 


时序越低  超频 潜力越大。

```



内存单通道，双通道

```
什么是双通道呢？
插 2条内存 是不是就是呢？
有什么有点呢？


--
如果主板上有2个内存条  插2条就是双通道


如果是4个卡槽  要按不同的顺序

12  为一个通道  34 为一个通道
 
13 23 14 23 24  都可以组成双通道。

一个4G 和一个 8G 可以组成双通道吗？

可以的   前半部分是双通道 后 4G 是单通道。


```



内存主流品牌

```
金士顿 芝奇  海盗船 英睿达 七彩虹 阿斯加特 。。。
```



Mac 上检测内存的软件工具 ATOMIC Mac

```
地址： http://www.pc6.com/mac/673707.html
```



挑选内存

```
内存容量> 双通道 > 频率 > 时序 > 品牌

```



![image-20210805113907435](/Users/apple/Library/Application Support/typora-user-images/image-20210805113907435.png)



虚拟内存地址

```
任何程序 或者二进制 运行时  都要先加载到内存之中 然后cpu 在执行指令。

在DOs 时期  是 实地址  用户进程可以修改内核东西 所以不安全

进程可以修改其他进程的内存情况，甚至 覆盖操作系统使用的内存。所以 出现了  保护模式。

进程不使用 实地址  而是 使用 虚拟内存地址，这些地址 被称为 线性地址

操作系统 负责 把 虚拟内存（线性地址） 映射 到 物理 内存 。


32位 系统中 每 4kb 作为一页。


操作系统以链表的形式记录 各个进程的控制信息。


页目录  页表  物理内存页

1024 * 1024 * 4KB=4GB


```



